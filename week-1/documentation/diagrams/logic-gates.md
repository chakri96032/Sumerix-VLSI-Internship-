# Logic Gate Diagrams

## 1. AND Gate

```text
A ────┐
      │ AND ─── Y
B ────┘

Y = A · B

A ────┐
      │ OR ─── Y
B ────┘

Y = A + B

A ────┐
      │ XOR ─── Y
B ────┘

Y = A ⊕ B

A ─── NOT ─── Y

Y = A'

A ────┐
      │ NAND ─── Y
B ────┘

Y = (A · B)'

A ────┐
      │ NOR ─── Y
B ────┘

Y = (A + B)'

A ────┐
      │ XNOR ─── Y
B ────┘

Y = (A ⊕ B)'